<?xml version="1.0" encoding="utf-8"?>
<!--
  Android 9 (API 28) and later block cleartext HTTP by default, which is why a
  build pointed at `http://<LAN IP>:8000` fails with
  "Cleartext HTTP traffic to ... not permitted" before the request ever leaves
  the phone.

  The fix is NOT to allow cleartext everywhere. This config keeps the secure
  default for every address — so a released build talking to the real backend
  must use HTTPS — and opens an explicit exception for the development hosts
  only.

  Working on a different network? Add your machine's LAN IP alongside the one
  below, or better, run against a deployed HTTPS backend and delete the entry.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />

    <domain-config cleartextTrafficPermitted="true">
        <!-- Android emulator's alias for the host machine's localhost. -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <!-- The development machine on the team's LAN. -->
        <domain includeSubdomains="false">192.168.254.168</domain>
    </domain-config>
</network-security-config>
